# Autotest

The featureless test toolkit for bash

- - -

## Chapter I: The origins

> Everything is nothing at origins

The very first step on this adventure starts with a git repo, and a test dir.

> $ cd /repos/autotest
> $ git init
> $ mkdir 0rigin
> $ touch 0rigin/0

- why `0rigin` and not `test`?
- Type `t` and press tab at your shell, how much possibilities? Here 220.

- why `0` then? an not `O` or `o`, or something like `first.test`
- Numbers `sort` before letters. `0` is the first number.

- - -

> $ vim 0rigin/0

```
```

- - -

> $ chmod +x 0rigin/0
> $ 0rigin/0 | egrep --color '^(TEST|FAIL|CODE)'

`grep --color` is an awesome report formater. Zero work and fits on one line.
