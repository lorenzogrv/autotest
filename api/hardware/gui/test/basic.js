var test = require('tape');

var server = require('../');

test('server prototype', function( t ){
  server.eventNames()
//  listenerCount()
  t.fail('write tests, stop cowboy coding');
  t.end();
})
