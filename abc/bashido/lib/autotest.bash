# no shebang because this file is meant to be sourced by bash
##

source "$(bashido abc-log)"
fail "autotest is not part of bashido now, use autotest directly"

# vim modeline
# /* vim: set filetype=sh ts=2 shiftwidth=2: */
